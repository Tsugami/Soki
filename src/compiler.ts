/* eslint-disable no-unused-vars */
import { Node } from './node'

class Compiler {
    AST: Node

    constructor (AST: Node) {
      this.AST = AST
    }

    compile () {

    }
}

export { Compiler }
